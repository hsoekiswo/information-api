{
  "name": "information-api",
  "scripts": {
    "dev": "bunx run src/index.ts",
    "build": "bunx build",
    "start": "bunx run src/index.ts",
    "migrate": "bunx prisma migrate deploy",
    "prisma:generate": "bunx prisma generate"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "@hono/swagger-ui": "^0.5.0",
    "@hono/zod-openapi": "^0.18.3",
    "dotenv": "^16.4.7",
    "hono": "^4.6.13",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@prisma/client": "^6.1.0",
    "prisma": "^6.1.0"
  }
}